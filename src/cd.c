#include "../includes/minishell.h"

/* int check_syntax_cd(t_main *main, char *cmd)
{
    
}

void    cd(t_main *main, char *cmd)
{
    if (check_syntax_cd(main, cmd) == 0)
        return ;
    
} */